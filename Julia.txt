# speed up start-up (just a little bit)

julia --startup-file=no -e 'using DaemonMode; serve()'&

# Julia dependencies (start julia, press ']' to get into a package manager,
then use 'add' to install all the packages):

DaemonMode ArgParse CodecBzip2 CodecLz4 ConfParser Downloads HTTP JSON LibPQ Tables SQLite WebSockets x265_jll ZfpCompression DistributedArrays FITSIO Images ImageTransformations Interpolations PhysicalConstants ThreadsX WCS

# running Julia on jvof or py1

julia -O3 -t 16 Julia/fitswebqlse.jl

# testing Julia

julia -p 2 -t 4 -O3 Julia/fitswebqlse.jl

# running Julia in a cluster

julia -O3 -t 4 --machine-file machines.txt Julia/fitswebqlse.jl

# an example config.ini

[fitswebql]
port=9000 ; optional
local=false ; optional
production=false ; optional
timeout=15 ; [s]
home=.cache ; optional
cache=.cahe ; optional

[postgresql]
user=jvo
password=??? ; optional
host=p10.vo.nao.ac.jp
port=5433 ; optional
fitshome=/home
